{"function_name": "statistics.correlation", "mutants": ["```python\n# property to violate: If one of the input lists is constant (e.g., all elements are the same), the function should raise a StatisticsError.\nfrom hypothesis import given, strategies as st\nimport statistics\n\n@given(st.lists(st.floats(), min_size=2))\ndef test_violation_of_statistics_correlation_1(x):\n    if len(set(x)) == 1:  # All elements are the same\n        correlation(x, [1.0] * len(x))\n        assert False  # Should not reach here\n\n@given(st.lists(st.floats(), min_size=2))\ndef test_violation_of_statistics_correlation_2(x):\n    if len(set(x)) == 1:  # All elements are the same\n        correlation(x, [2.0] * len(x))\n        assert False  # Should not reach here\n\n@given(st.lists(st.floats(), min_size=2))\ndef test_violation_of_statistics_correlation_3(x):\n    if len(set(x)) == 1:  # All elements are the same\n        correlation(x, [0.0] * len(x))\n        assert False  # Should not reach here\n\n@given(st.lists(st.floats(), min_size=2))\ndef test_violation_of_statistics_correlation_4(x):\n    if len(set(x)) == 1:  # All elements are the same\n        correlation(x, [3.0] * len(x))\n        assert False  # Should not reach here\n\n@given(st.lists(st.floats(), min_size=2))\ndef test_violation_of_statistics_correlation_5(x):\n    if len(set(x)) == 1:  # All elements are the same\n        correlation(x, [5.0] * len(x))\n        assert False  # Should not reach here\n```"], "property": "If one of the input lists is constant (e.g., all elements are the same), the function should raise a StatisticsError.", "pbt": "@given(st.lists(st.floats(), min_size=2))\ndef test_constant_list_property(x):\n    if len(set(x)) == 1:  # All elements are the same\n        try:\n            correlation(x, [1.0] * len(x))\n            assert False  # Should not reach here\n        except StatisticsError:\n            assert True  # Expected behavior", "properties": ["3. If one of the input lists is constant (e.g., all elements are the same), the function should raise a StatisticsError."]}
